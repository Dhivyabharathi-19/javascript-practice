// Convert a Set back into an array using Array.from().

const vehiclesSet = new Set(["car", "bike", "bus"]);

const vehiclesArray = Array.from(vehiclesSet);

console.log(vehiclesArray);

//output:[ 'car', 'bike', 'bus' ]
