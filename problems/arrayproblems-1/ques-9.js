// Reverse an array without modifying the original.

let names = ["tom", "jerry", "dora"];

let reversed = [...names].reverse();

console.log(reversed);
console.log(names);

// output:
// ['dora', 'jerry', 'tom' ]
// [ 'tom', 'jerry', 'dora' ]
