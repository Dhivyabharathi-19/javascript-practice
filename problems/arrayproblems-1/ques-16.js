// Convert an array of strings to uppercase.

let fruits = ["apple", "banana", "strawberry"];

let up = fruits.map((fruits) => fruits.toUpperCase());

console.log(up);

// output:[ 'APPLE', 'BANANA', 'STRAWBERRY' ]
