// Print the number of entries in the Map using .size.

const studentMarks = new Map();

studentMarks.set("Alice", 85);
studentMarks.set("Bob", 92);
studentMarks.set("Charlie", 78);

console.log("Number of students:", studentMarks.size);

// output:Number of students: 3
